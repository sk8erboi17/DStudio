GSA Phase 3/4: validation.
Workspace root: `{{WORKDIR}}`.
Use `{{RUN_DIR}}/preflight.json`, `{{RUN_DIR}}/validation-plan.json`, `{{RUN_DIR}}/validation-results.json`, and `{{RUN_DIR}}/evidence-graph.json`. {{VALIDATION_CONTEXT_RULE}}`{{RUN_DIR}}/tool-retry-policy.md`, `{{RUN_DIR}}/tool-retry-ledger.jsonl`, `{{RUN_DIR}}/workbench.json`, and `{{RUN_DIR}}/workbench.md`.
The backend validation executor has already run the structured plan where possible; treat its output as advisory evidence to interpret, confirm, reject, or supplement with bounded source review. Append any additional external-tool invocation failures to tool-retry-ledger.jsonl and normalize reusable observations into the matching workbench-*.jsonl file.
{{VALIDATION_PROFILE_RULE}}{{NETWORK_SCOPE_RULE}}{{BLACKHAT_VOICE_RULE}}
`gsa-task.json` lives at the Workspace root above, not in the GSA run artifact directory. Protocol hygiene: do not search or cite `.dstudio/gsa/runs/*.prompt.md`; prompt files are control data, not evidence.

Confirmation gate: for every would-be `confirmed_issue`, verify all five links before assigning medium/high confidence: defect or missing control, attacker-controlled or untrusted input/data source, realistic attacker capability, propagation or downstream consumer, and demonstrated impact.
For exported cryptographic, token, signature, serializer, parser, or policy primitives, caller-controlled function parameters plus tests/config/artifacts showing intended use can satisfy links (2) and (4) for a bounded code-level finding. When that local-source exception applies, preserve the finding at medium/high confidence; put missing production caller/consumer/route/service wiring in `missing_evidence` instead of downgrading solely for that absence.
In `authorized-local-source-review` of a package/library, the exported public API is itself a valid boundary; do not require an HTTP route, controller, CLI, or service main if the workspace did not include one.
A generic `open(path)`, CSV writer, string parser, or loader is not a confirmed vulnerability unless a reachable caller or artifact shows attacker influence over that argument and a concrete leak/write/execution/output consumer.

Use `tool-retry-policy.md` for all selected-tool invocation failures. A clean or empty scanner result is not proof of safety by itself; only close a hypothesis when code/artifact evidence also shows the relevant control or unreachable path. Positive tool output is advisory too; tie it to a cited reachable code path, artifact, request flow, trace or concrete consumer before reporting it. Scripts or external commands that are still only planned are incomplete evidence.
When a helper script fails because a selected path moved or was guessed wrong, make at most one repair attempt, then record the failure in scripts_manifest.json and keep the hypothesis incomplete unless other evidence proves it.
Append concrete evidence to `{{RUN_DIR}}/evidence.jsonl`; include file:line, script output path, HTTP status, or artifact excerpts. Do not use `edit` on evidence.jsonl; append only.
When validation uses web, network, forensics, reverse, code, infra or tool-run output, add a workbench row and cite the relevant workbench row from evidence.jsonl when it supports a finding.
For crypto/signature validation, check and cite each relevant control or gap: constant-time tag comparison, key material versus key reference, env/key-id registry binding, nonce uniqueness/replay policy, canonical request normalization, and audit/config policy.
Validate attack chains explicitly: if multiple weaker facts compose into impact, cite each link in order and explain the transition; if one link is missing or contradicted, keep the chain `inconclusive` with that missing link.

Read at most 6 extra files beyond the selected scope. Add at most 6 new evidence lines. Keep the output to at most 2 validated security findings or one explicit no-issue/inconclusive evidence trail. Do not use `severity":"none"`.
Output compact JSON only, no markdown fences, no preamble, no postscript, no analysis prose.
Output JSON only to be saved as validation.json:
{"phase":"validation","findings":[{"title":"...","severity":"low|medium|high|critical","evidence":["file:line"],"exploit_path":"...","impact":"...","confidence":"low|medium|high","missing_evidence":"...","attack_chain":["optional ordered evidence-backed chain link"]}],"validationExecutor":{"status":"complete|partial|skipped","summary":{"steps":1,"complete":1,"failed":0,"skipped":0},"steps":[{"id":"step-1-semgrep_scan","adapter":"semgrep_scan","status":"complete|failed|skipped","tool":"semgrep","summary":"..."}],"artifacts":["validation-plan.json","validation-results.json","evidence-graph.json"]},"evidenceGraph":{"nodes":1,"edges":1,"missingLinks":["..."]},"workbench":{"domains":["code"],"artifacts":["workbench-code.jsonl"],"toolRuns":1,"notable":["..."]}}
